import { PolicyService } from '../../types/policyServiceType';
import { Language } from '../../types/languageType';
export declare const serializePolicyService: (policyService: PolicyService, lang: Language) => {
    content: string;
    id: number;
    createdAt: Date;
    updatedAt: Ddaate;
};
