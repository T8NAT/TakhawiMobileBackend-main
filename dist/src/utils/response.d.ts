import { Response } from 'express';
import { ResponseType } from '../types/responseType';
declare const _default: (res: Response, statusCode: number, data: ResponseType) => void;
export default _default;
